{% extends 'ValBundle::layout.html.twig' %}

{% block title %}{{ 'register.new.title'|trans }}{% endblock %}

{% block content_body %}
<div class="container">
    {{ form(form) }}
</div>
<div class="container">
<a href="{{ path('register') }}" class="btn btn-default">{{ 'btn_com.index'|trans }}}</a>
</div>
{% endblock %}
